# Error Types - Visual Comparison Guide

## Database Errors - Visual Examples

### 1. User Not Found (404)

```
┌─────────────────────────────────┐
│ BACKEND THROWS                  │
├─────────────────────────────────┤
│ DatabaseNotFoundError {         │
│   message: "User 123 not found" │
│   entityType: "User"            │
│   entityId: "123"               │
│ }                               │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ FRONTEND RECEIVES               │
├─────────────────────────────────┤
│ HTTP 404                        │
│ message: "The requested User    │
│           was not found."       │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ USER SEES                       │
├─────────────────────────────────┤
│ ⚠️ The requested User was not   │
│    found.                       │
└─────────────────────────────────┘
```

### 2. Duplicate Email (409)

```
┌─────────────────────────────────┐
│ BACKEND THROWS                  │
├─────────────────────────────────┤
│ DatabaseDuplicateError {        │
│   message: "Email exists"       │
│   field: "email"                │
│   value: "user@example.com"     │
│ }                               │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ FRONTEND RECEIVES               │
├─────────────────────────────────┤
│ HTTP 409                        │
│ message: "This email is already │
│          in use. Please use a   │
│          different value."      │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ USER SEES (Form Field)          │
├─────────────────────────────────┤
│ Email field:                    │
│ ❌ This email is already in use.│
│    Please use a different value.│
│                                 │
│ [User enters new email]         │
│ [Submit form again]             │
└─────────────────────────────────┘
```

### 3. Database Connection Failed (503)

```
┌─────────────────────────────────┐
│ BACKEND THROWS                  │
├─────────────────────────────────┤
│ DatabaseConnectionError {       │
│   message: "ECONNREFUSED"       │
│   serviceName: "PostgreSQL"     │
│ }                               │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ FRONTEND RECEIVES               │
├─────────────────────────────────┤
│ HTTP 503 Service Unavailable    │
│ message: "Database service is   │
│          temporarily unavailable│
│          Try again later."      │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ USER SEES                       │
├─────────────────────────────────┤
│ ⚠️ Database service is          │
│    temporarily unavailable.     │
│    Please try again later.      │
│                                 │
│ [Retry button]                  │
└─────────────────────────────────┘
```

---

## Network Errors - Visual Examples

### 1. External Service Error (503)

```
REQUEST TO BACKEND
        │
        ▼
BACKEND CALLS CAPTCHA API
        │
        ├─ If Down (503)
        │
        ▼
BACKEND THROWS
┌────────────────────────────────┐
│ ExternalServiceError {         │
│   serviceName: "CaptchaService"│
│   statusCode: 503              │
│ }                              │
└────────────┬───────────────────┘
             │
             ▼
HTTP 503 TO FRONTEND
             │
             ▼
FRONTEND SHOWS
┌────────────────────────────────┐
│ ⚠️ CaptchaService is           │
│    temporarily unavailable.    │
│    Please try again later.     │
│                                │
│ [Retry button]                 │
│ [Try alternative method]       │
└────────────────────────────────┘
```

### 2. Request Timeout (504)

```
FRONTEND MAKES REQUEST WITH 5s TIMEOUT
        │
        ├─ < 5s ✓ Response received
        │
        └─ > 5s ✗ Timeout
                  │
                  ▼
        ABORT SIGNAL SENT
                  │
                  ▼
        CATCH ABORT ERROR
                  │
                  ▼
        THROW TimeoutError {
          timeoutMs: 5000,
          endpoint: "https://..."
        }
                  │
                  ▼
        HTTP 504 TO FRONTEND
                  │
                  ▼
        FRONTEND SHOWS
        ┌─────────────────────┐
        │ ⏱️ Request took too │
        │    long. Please try │
        │    again.           │
        │                     │
        │ [Retry button]      │
        └─────────────────────┘
```

### 3. JSON Parse Error (502)

```
RESPONSE RECEIVED
        │
        ▼
FRONTEND TRIES: response.json()
        │
        ├─ Valid JSON ✓ Parse successful
        │
        └─ Invalid JSON ✗ Parse fails
                          │
                          ▼
                THROW ParseError {
                  type: "json"
                }
                          │
                          ▼
                HTTP 502 BAD GATEWAY
                          │
                          ▼
                FRONTEND SHOWS
                ┌──────────────────────┐
                │ ⚠️ Failed to process │
                │    response from the │
                │    server. Please    │
                │    try again.        │
                │                      │
                │ [Retry button]       │
                └──────────────────────┘
```

---

## Authentication Errors - Visual Examples

### 1. Invalid Credentials (401)

```
USER ENTERS: user@example.com / password123
        │
        ▼
BACKEND validateUser()
        │
        ├─ Find user in DB
        │  └─ User NOT found ✗
        │     └─ THROW InvalidCredentialsError
        │
        └─ OR password DOESN'T match ✗
           └─ THROW InvalidCredentialsError
                  │
                  ▼
        BACKEND RETURNS HTTP 401
                  │
                  ▼
        FRONTEND SHOWS
        ┌──────────────────────────┐
        │ ❌ Invalid email or      │
        │    password.             │
        │                          │
        │ Email: [ _ _ _ _ _ _ ]   │
        │ Password: [ *** ]        │
        │           [Login]        │
        └──────────────────────────┘

        SECURITY NOTE: Same message for both
        "user not found" and "password wrong"
        (prevents user enumeration)
```

### 2. Token Expired (401)

```
USER VISITS APP WITH EXPIRED TOKEN
        │
        ▼
JWT STRATEGY VALIDATES
        │
        ├─ Token valid ✓ Allow access
        │
        └─ Token expired ✗
           THROW TokenExpiredError {
             tokenType: "access"
           }
                  │
                  ▼
        BACKEND RETURNS HTTP 401
                  │
                  ▼
        FRONTEND DETECTS CODE: "AUTH_TOKEN_EXPIRED"
                  │
                  ▼
        OPTION 1: Try refresh token
                  │
                  ├─ Refresh succeeds ✓
                  │  └─ Continue (silent)
                  │
                  └─ Refresh fails ✗
                     └─ Continue to option 2
                  │
                  ▼
        OPTION 2: Redirect to login
        ┌──────────────────────────┐
        │ ⏰ Your session has      │
        │    expired. Please log   │
        │    in again.             │
        │                          │
        │ [Login button]           │
        └──────────────────────────┘
```

### 3. Account Locked (429)

```
USER FAILS LOGIN 5 TIMES
        │
        ▼ (Each failed attempt recorded)
        │
ATTEMPT #5: FAILS
        │
        ▼
BACKEND recordFailedAttempt()
        │
        ├─ Attempts >= 5
        │
        └─ THROW AccountLockedError {
             lockedUntil: "2025-11-01T15:30Z"
             attemptsAllowed: 5
           }
                  │
                  ▼
        BACKEND RETURNS HTTP 429 TOO MANY REQUESTS
                  │
                  ▼
        FRONTEND CALCULATES COUNTDOWN

        Locked: 15:20:00 ← current time
        Until:  15:30:00

        FRONTEND SHOWS
        ┌────────────────────────────┐
        │ 🔒 Too many login attempts.│
        │    Please try again in     │
        │    9:59                    │
        │                            │
        │ [Countdown timer updates]  │
        │ [Once reached, Enable form]│
        └────────────────────────────┘
```

---

## Validation Errors - Visual Example

### Form Validation (400)

```
USER SUBMITS FORM
┌──────────────────┐
│ Email: abc       │ ← Invalid
│ Password: 123    │ ← Too short
└──────────────────┘
        │
        ▼
BACKEND VALIDATES WITH ZOD
        │
        ├─ Email: "Invalid email format"
        ├─ Password: "Must be 8+ characters"
        │
        ▼
THROW ValidationError {
  issues: [
    { path: ["email"], message: "Invalid email format" },
    { path: ["password"], message: "Must be 8+ characters" }
  ]
}
        │
        ▼
BACKEND RETURNS HTTP 400
        │
        ▼
FRONTEND RENDERS
┌─────────────────────────────────┐
│ Email field:                    │
│ [abc           ]                │
│  ❌ Invalid email format        │
│                                 │
│ Password field:                 │
│ [••••        ]                  │
│  ❌ Must be 8+ characters       │
│                                 │
│ [Submit button (disabled)]      │
│                                 │
│ AS USER TYPES:                  │
│ Email: [user@example.com]       │
│ Password: [••••••••••••]        │
│  Submit button (enabled)        │
└─────────────────────────────────┘
```

---

## Error Code to HTTP Status Mapping

```
┌─────────────────────────────────────────────────────┐
│ 400 - BAD REQUEST (Client Error)                    │
├─────────────────────────────────────────────────────┤
│ • VALIDATION_FAILED          (ValidationError)      │
│ • BAD_REQUEST                (BadRequestError)      │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ 401 - UNAUTHORIZED (Authentication Error)           │
├─────────────────────────────────────────────────────┤
│ • AUTH_INVALID_CREDENTIALS   (InvalidCredentials)   │
│ • AUTH_UNAUTHORIZED          (UnauthorizedError)    │
│ • AUTH_TOKEN_EXPIRED         (TokenExpiredError)    │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ 403 - FORBIDDEN (Authorization Error)               │
├─────────────────────────────────────────────────────┤
│ • AUTH_FORBIDDEN             (ForbiddenError)       │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ 404 - NOT FOUND (Resource Error)                    │
├─────────────────────────────────────────────────────┤
│ • DB_NOT_FOUND               (DatabaseNotFound)     │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ 409 - CONFLICT (Data Conflict)                      │
├─────────────────────────────────────────────────────┤
│ • DB_DUPLICATE_ENTRY         (DatabaseDuplicate)    │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ 429 - TOO MANY REQUESTS (Rate Limit)                │
├─────────────────────────────────────────────────────┤
│ • AUTH_ACCOUNT_LOCKED        (AccountLockedError)   │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ 500 - INTERNAL SERVER ERROR (Server Error)          │
├─────────────────────────────────────────────────────┤
│ • DB_QUERY_FAILED            (DatabaseQueryError)   │
│ • CONFIG_INVALID             (ConfigValidation)     │
│ • CONFIG_MISSING             (MissingConfigError)   │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ 502 - BAD GATEWAY (Malformed Response)              │
├─────────────────────────────────────────────────────┤
│ • PARSE_ERROR                (ParseError)           │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ 503 - SERVICE UNAVAILABLE (Temporary Outage)        │
├─────────────────────────────────────────────────────┤
│ • DB_CONNECTION_FAILED       (DatabaseConnection)   │
│ • EXTERNAL_SERVICE_FAILED    (ExternalService)      │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ 504 - GATEWAY TIMEOUT (Response Too Slow)           │
├─────────────────────────────────────────────────────┤
│ • REQUEST_TIMEOUT            (TimeoutError)         │
└─────────────────────────────────────────────────────┘
```

---

## What Goes Where - Quick Reference

| Scenario                             | Error Type                | HTTP   | File Location                 |
| ------------------------------------ | ------------------------- | ------ | ----------------------------- |
| User enters wrong password           | `InvalidCredentialsError` | 401    | `auth/index.ts`               |
| User trying to access another's data | `ForbiddenError`          | 403    | `auth/index.ts`               |
| User queries for non-existent record | `DatabaseNotFoundError`   | 404    | `database/not-found.error.ts` |
| Duplicate email on registration      | `DatabaseDuplicateError`  | 409    | `database/index.ts`           |
| Cannot connect to database           | `DatabaseConnectionError` | 503    | `database/index.ts`           |
| Request form has invalid data        | `ValidationError`         | 400    | `validation/index.ts`         |
| Network request fails                | `HttpError`               | varies | `network/index.ts`            |
| Cannot parse JSON response           | `ParseError`              | 502    | `network/index.ts`            |
| Captcha service is down              | `ExternalServiceError`    | 503    | `network/index.ts`            |
| API call times out                   | `TimeoutError`            | 504    | `network/index.ts`            |
| Too many failed login attempts       | `AccountLockedError`      | 429    | `auth/index.ts`               |
| JWT token expired                    | `TokenExpiredError`       | 401    | `auth/index.ts`               |

---

## Error Flow Diagram - Complete Picture

```
┌──────────────────────────────────────────────────────────────────┐
│                      APPLICATION LAYERS                          │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────┐
│      FRONTEND (React)        │
│  ┌─────────────────────────┐ │
│  │ User Action(form submit)│ │
│  └────────────┬────────────┘ │
│               │              │
│               ▼              │
│  ┌─────────────────────────┐ │
│  │ fetch() call            │ │
│  │ (HTTP request)          │ │
│  └────────────┬────────────┘ │
└──────────────────────────────┘
                 │
                 ▼ HTTP REQUEST
┌───────────────────────────────────────────────────────┐
│     BACKEND (NestJS)                                  │
│  ┌─────────────────────────┐                          │
│  │ Controller receives     │                          │
│  │ and validates request   │                          │
│  └────────────┬────────────┘                          │
│               │                                       │
│    ┌──────────┴──────────┐                            │
│    │                     │                            │
│    ▼                     ▼                            │
│ ┌─────────────┐   ┌──────────────────┐                |
│ │ Valid       │   │ Invalid          │                |
│ │ Call Service│   │ Throw Error      │                |
│ └─────────────┘   └────────┬─────────┘                |
│                            │                          |
│                            ▼                          |
│                   ┌──────────────────────┐            |
│                   │ Which error?         │            |
│                   └──────────┬───────────┘            |
│                              │                        |
│         ┌────────┬───────┬───┴──┬─────────┐           |
│         │        │       │      │         │           |
│         ▼        ▼       ▼      ▼         ▼           |
│    DB Error  Auth Error Network Validation Config     |
│         │        │       │      │         │           |
│         └────────┴───┬───┴──────┴─────────┘           |
│                      │                                |
│                      ▼                                |
│              ┌────────────────────┐                   |
│              │ mapCustomError     │                   |
│              │ ToNestException()  │                   |
│              └────────┬───────────┘                   |
│                       │                               |
│                       ▼                               |
│              ┌────────────────────┐                   |
│              │ throw HttpException│                   |
│              │ (401, 404, 503...) │                   |
│              └────────┬───────────┘                   |
└───────────────────────────────────────────────────────┘
                 │
                 ▼ HTTP RESPONSE (with status code)
┌──────────────────────────────┐
│      FRONTEND (React)        │
│  ┌─────────────────────────┐ │
│  │ Receive HTTP response   │ │
│  │ (status: 401)           │ │
│  └────────────┬────────────┘ │
│               │              │
│               ▼              │
│  ┌─────────────────────────┐ │
│  │ Convert to custom error │ │
│  │ new InvalidCredentials()│ │
│  └────────────┬────────────┘ │
│               │              │
│               ▼              │
│  ┌─────────────────────────┐ │
│  │ Access error.metadata   │ │
│  │ userMessage             │ │
│  └────────────┬────────────┘ │
│               │              │
│               ▼              │
│  ┌─────────────────────────┐ │
│  │ Display to user         │ │
│  │ "Invalid email or..."   │ │
│  └─────────────────────────┘ │
└──────────────────────────────┘
```
